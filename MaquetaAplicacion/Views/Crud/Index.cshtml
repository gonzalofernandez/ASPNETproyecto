@model MaquetaAplicacion.Models.Referencia

@{
    ViewBag.Title = "Admin";
}

<h2>Índice de Referencias</h2>

<p>
    @Html.ActionLink("Crear referencia", "Create")
</p>
<table class="table text-center">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var referencia in @ViewBag.Referencias) {
    <tr>
        <td>
            @referencia.Nombre
        </td>
        <td>
            @referencia.Descripcion
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Detalles", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Eliminar", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
